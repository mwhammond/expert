



<!-- ADVISORS -->

<div id="innerpage">
  <div class="container">
    <div class="row mt">
      <div class="col-lg-8">

        <h1>Admin mode</h1>



      </div>

    </div>


    <div class="row mt">

      <h2>Startups</h2>

      <br>

      <% if can? :update, @startup %>

      <h3><%= link_to '+ Add new startup', new_startup_path %></h3>

      <% end %>

      <% if @startups %> 
      <% @startups.each do |startup| %>

      <div class="col-lg-3">

        <%= image_tag startup.avatar.url(:medium), class: "img-responsive" %>
        <h3><%= startup.name %></h3>
        <p><b><%= startup.sector %></b><p>
          <p><%= startup.bio %></p>
          <p><%= startup.requests %></p>


          <% if can? :update, @startup %>

          <p><%= link_to 'Edit', edit_startup_path(startup) %> | 
            <%= link_to 'Delete', startup_path(startup),
            method: :delete, data: { confirm: 'Are you sure?' } %> </p>

            <% end %>

          </div>

          <%end%>

          <%else%>
          <p>We seem to have temporarily lost the records?</p>
          <%end%>


          <div class="row mt">
            <div class="col-lg-12">
              <br>



            </div>    
          </div>

          <br>
          <br>


<div class="row mt">

      <h2>Advisors</h2>

      <br>

      <% if can? :update, @advisors %>

      <h3><%= link_to '+ Add new advisor', new_advisor_path %></h3>

      <% end %>

      <% if @advisors %> 
      <% @advisors.each do |advisor| %>

      <div class="col-lg-3">

        <%= image_tag advisor.avatar.url(:medium), class: "img-responsive" %>
        <h3><%= advisor.name %></h3>


          <% if can? :update, @startup %>

          <p><%= link_to 'Edit', edit_startup_path(startup) %> | 
            <%= link_to 'Delete', startup_path(startup),
            method: :delete, data: { confirm: 'Are you sure?' } %> </p>

            <% end %>

          </div>

          <%end%>

          <%else%>
          <p>We seem to have temporarily lost the records?</p>
          <%end%>


          <div class="row mt">
            <div class="col-lg-12">
              <br>



            </div>    
          </div>

          <br>
          <br>

        </div>
      </div>  
      </div>  

